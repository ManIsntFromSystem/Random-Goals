<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.quantumman.randomgoals">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:fullBackupContent="@xml/backup_descriptor">
        <activity android:name=".app.views.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".app.views.EditItemsListActivity"
            android:label="Edit Goals"
            android:screenOrientation="portrait"
            android:parentActivityName=".app.views.ListGoalsActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".app.views.ListGoalsActivity"/>
        </activity>
        <activity
            android:name=".app.views.ListGoalsActivity"
            android:screenOrientation="portrait"/>
        <provider
            android:authorities="com.quantumman.randomgoals"
            android:name=".data.GoalsContentProvider"
            android:exported="false"/>
    </application>

</manifest>